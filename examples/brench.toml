# which benchmarks to run
benchmarks = '../benchmarks/**/*.bril'
# how to extract the performance metric from stderr
extract = 'total_dyn_inst: (\d+)'

[runs.baseline]
pipeline = [
  "bril2json",
  "brili -p {args}",
]

[runs.ssa]
pipeline = [
  "bril2json",
  "python3 to_ssa_ani.py",
  "brili -p {args}",
]

[runs.ref_ssa]
pipeline = [
  "bril2json",
  "python3 ssa_w_loops_ani.py",
  "brili -p {args}",
]
